/*import express from 'express';
var usersRouter = express.Router();



usersRouter.get('/', function(req, res, next) {
  res.send('respond with a resource');
});


module.exports.fibonacciAsync = function(n, done){ = expose the object
  if(n === 0) done(undefined, 0)
}

exports.fibonacciAsync = expose the property 

npm install lodash -g //install globally

npm install --production //this command only install package from dependencies object

m = require()
m.fibonacciAsync()

export {usersRouter}


chaining of promises
fetch().then(response=>{}).then().then().catch(err=>{})

const doSomethingAsync = () => {
  return new Promise((resolve, reject)=>{

  });
}

const doSomething = async () => {
  console.log(await doSomethingAsync());
}


Event emitter
const eventEmitter = require('events')
eventEmitter.emit('')

function test(){
  eventEmitter.emit()
}

class Pulser extends EventEmitter{

}

module.expor

require used in common js module
  -load synchroniously 
  -only used in common js module
  -when load we get complete object
  -common js module can not load es6 module
   (you can not use import in common js module)
   you have to use import() function to load  es6 module
   import().then(file=>)
import used in es6 module
  -use to load es6 module
  -onnly use inside es6 module
  -es6 module can have multiple named exports 
  export nfunction next, exprt dunction squared
  export default function

  with es6 module we caqn only import required thing
  import {default as simppe, hello, next}
  import simple 
  es6 module can also import common js module
  import simple from './js/'*/